<Window x:Class="KontaktBuchApp.Views.ContactMethodView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KontaktBuchApp.Views"
				 xmlns:Control="clr-namespace:KontaktBuchApp.Themes"
             mc:Ignorable="d" 
             d:DesignHeight="450"
				 d:DesignWidth="800">
	<StackPanel Margin="20">

		<TextBlock Text="Email"/>
		<TextBox Text="{Binding Email}" />

		<TextBlock Text="Phone"/>
		<TextBox Text="{Binding Phone}" />

		<TextBlock Text="Fax"/>
		<TextBox Text="{Binding Fax}" />

		<TextBlock Text="Website"/>
		<TextBox Text="{Binding Website}" />

		<TextBlock Text="Mobile"/>
		<TextBox Text="{Binding Mobile}" />

		<StackPanel Orientation="Horizontal"
              HorizontalAlignment="Right"
              Margin="0,20,0,0">

			<Control:IconButton Text="OK"
              Width="80"
              Margin="5"
              Command="{Binding OkCommand}" />

			<Control:IconButton Text="Cancel"
              Width="80"
              Margin="5"
              Command="{Binding CancelCommand}" />

		</StackPanel>

	</StackPanel>
</Window>
